# Environment
NODE_ENV=development
PORT=5000

# Database (PostgreSQL)
# Use the user you created earlier: safar_user / password: Aman
DATABASE_URL=postgresql://safar_user:Aman@localhost:5432/safar_sathi

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
# If you set a password in Redis config, put it here. Otherwise, leave blank.
REDIS_PASSWORD=
REDIS_DB=0

# Authentication
# ⚠️ Change this to a long, random string (at least 32 characters).
# You can use: openssl rand -hex 32
JWT_SECRET=super-long-random-secret-change-this-1234567890abcdef

# Frontend (CORS)
# Local React = 3000, Vite = 5173
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
